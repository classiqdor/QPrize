# Session 2026-03-29 18:28 — Claude (continuation)

## Context
Resuming from prior session (worklog/2026-03-29_1800_claude.md). The main outstanding task is reducing the 6-bit CX count from 1252 to ≤ 500 for hardware viability. Prior session identified the Mersenne custom adder as the top opportunity.

## Work Done

### attempt_006: quantum-quantum modular add for x1

Created `attempts/attempt_006_2026-03-29_1828.py`.

**Key insight implemented:**
- g_steps = [1, 2, 4, 8, 16, ...] = standard binary weights
- Therefore x1's total contribution to ecp = integer value of x1 (mod n)
- Replace 5 controlled constant additions for x1 with ONE `modular_add_inplace(n, x1, ecp)` call
- This insight generalizes to all bit sizes

**Circuit structure:**
- ecp in computational basis (no QFT encoding)
- x1 as QNum → `modular_add_inplace(n, x1, ecp)` (quantum-quantum)
- x2 as QArray[QBit] → bit-by-bit `control(x2[i], modular_add_constant_inplace(n, negq_steps[i], ecp))`
- Inverse QFT on x1, x2 for period finding

**Expected improvement:**
| Part | Old CX | New CX | Saving |
|------|--------|--------|--------|
| x1 contribution | 5 × 110 = 550 | ~175 (one q-q add) | ~375 |
| x2 contribution | 5 × 120 = 598 | 598 (unchanged) | 0 |
| QFT / overhead | 104 | ~60 (no ecp QFT) | ~44 |
| **Total** | **1252** | **~833** | **~419** |

**Status:** Written, NOT yet run. Needs synthesis + execution to verify.

## Open Questions / Next Steps

1. Run attempt_006 for 4-bit first (fast sanity check), then 6-bit
2. If CX is ~833 as estimated: attempt_007 should tackle x2 optimization
   - Option: use `modular_add_inplace` for x2 too, after precomputing weighted sum
   - Option: exploit 26=2*13, 22=2*11 doubling structure in negq_steps
   - Option: quantum ROM lookup for x2_weighted
3. If `modular_add_inplace` has higher CX than estimated: reconsider approach
4. Target: ≤ 500 CX for 6-bit hardware runs
