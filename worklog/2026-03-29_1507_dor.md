# Session 2026-03-29 ~12:00–15:07 — dor (Claude)

## Done
- Created `CLAUDE.md` with full setup instructions (fish shell, venv, classiq-library)
- Set up Python venv, installed all requirements
- Wrote `consts.py` with all competition curve parameters (bits 4–21)
- Wrote `utils.py` with `timed()` context manager and `play_ending_sound()` (Linux+Mac)
- Created attempt_001 (naive x,y approach — immediately obsolete)
- Created attempt_002 / attempt_002B (group-index encoding baseline, exports solve())
- Created attempt_003 (optimization_level=1 — no improvement over 002B, same 716 CX for 4-bit)
- Built testing framework: `attempts/registry.py` (append-only), `test_attempts.py`, `pytest.ini`
- Created `test_validation.py` with algebraic (instant) + quantum + code-review tests
- Created `code_review.md` — mathematical code review result: APPROVED
- Created `README.md`, pushed everything to https://github.com/classiqdor/QPrize
- Created `PLAN.md` with strategic roadmap and multi-agent coordination note
- Created `GUIDELINE.md` with working conventions for all contributors
- Created **attempt_004** — correct ECDLP implementation using actual EC arithmetic

## Key discovery: attempts 002-003 were wrong
- "Group-index encoding" required knowing `d` to build the oracle: `neg_q_step = (n-d) % n`
- This is circular — the algorithm was solving "given d, find d"
- attempt_004 fixes this by using actual EC point arithmetic (x,y coordinates mod p)
- Oracle: `ecp = P0 + x1*G - x2*Q` using classically precomputed point doublings of G and Q

## Failed / Abandoned
- optimization_level=1 (attempt_003): no improvement for 4-bit (same 716 CX)
- Group-index encoding approach: fundamentally incorrect for ECDLP (see above)

## Known issues in tests
- `test_algebraic_postprocessing` uses the wrong formula convention (swapped x1/x2) but only
  tests 4-bit (degenerate case d=n-1) so it still passes. Fix: update to use correct formula.
- The reference Classiq notebook uses `d = -x1_r * x2_r^{-1}` which is wrong for non-degenerate
  cases. Correct formula: `d = -x2_r * x1_r^{-1}`, filter `gcd(x1_r, n) == 1`.

## Open questions / Next steps
- Run attempt_004 to get qubit/CX counts and verify d is recovered correctly
- If CX count is too high for hardware: implement semiclassical QFT (Priority 1 in PLAN.md)
- Update registry.py after attempt_004 is verified
- Fix test_algebraic_postprocessing formula
