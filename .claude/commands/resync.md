Run `git pull --rebase` and then read the key project files to get fully up to date.

Steps:
1. Run `git pull --rebase` and report what changed (new commits, files updated).
2. Read `GUIDELINE.md` in full.
3. Read `PLAN.md` in full.
4. Read `attempts/registry.py`.
5. Read `attempts/RESULTS.md`.
6. Read the most recent file in `worklog/` (sort by filename descending, take the first).
7. Summarize what you learned: any new attempts registered, CX changes, open TODOs from PLAN.md, and any decisions or context from the latest worklog entry.
